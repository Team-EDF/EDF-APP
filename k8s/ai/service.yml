apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: edf
  labels:
    app: ai
spec:
  type: ClusterIP
  selector:
    app: ai
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000